diff --git a/src/app/components/atleta-component/atleta-component.ts b/src/app/components/atleta-component/atleta-component.ts
index bc5b6be..211696b 100644
--- a/src/app/components/atleta-component/atleta-component.ts
+++ b/src/app/components/atleta-component/atleta-component.ts
@@ -31,9 +31,9 @@ export class AtletaComponent {
     pessoaAtleta.nome = this.nome;
     pessoaAtleta.cpf = this.cpf;
     pessoaAtleta.sexo = this.sexo;
-    pessoaAtleta.dataNascimento = this.dataNascimento;
+    pessoaAtleta.data_nascimento = this.dataNascimento;
     pessoaAtleta.cep = Number(this.cep);
-    pessoaAtleta.rua = this.rua;
+    pessoaAtleta.rua_logradouro = this.rua;
     pessoaAtleta.bairro = this.bairro;
     pessoaAtleta.cidade = this.cidade;
     pessoaAtleta.uf = this.uf;
diff --git a/src/app/components/inscricao-component/inscricao-component.html b/src/app/components/inscricao-component/inscricao-component.html
index bad9118..38b075c 100644
--- a/src/app/components/inscricao-component/inscricao-component.html
+++ b/src/app/components/inscricao-component/inscricao-component.html
@@ -8,8 +8,8 @@
       <label for="atleta">Atleta</label>
       <select id="atleta" name="atleta" [(ngModel)]="idAtleta" required>
         <option [ngValue]="null" disabled>Selecione um atleta</option>
-        @for (atleta of atletas(); track atleta.idAtleta) {
-          <option [ngValue]="atleta.idAtleta">{{ atleta.nome }}</option>
+        @for (atleta of atletas(); track atleta.id) {
+          <option [ngValue]="atleta.id">{{ atleta.nome }}</option>
         }
       </select>
 
diff --git a/src/app/components/inscricao-component/inscricao-component.ts b/src/app/components/inscricao-component/inscricao-component.ts
index dcd2de8..3f32159 100644
--- a/src/app/components/inscricao-component/inscricao-component.ts
+++ b/src/app/components/inscricao-component/inscricao-component.ts
@@ -69,7 +69,7 @@ export class InscricaoComponent implements OnInit {
 
     this.inscricaoService.listar().subscribe(inscricoes => {
       const lista = inscricoes.flatMap((inscricao) => {
-        const atleta = atletas.find((item) => item.idAtleta === inscricao.idAtleta);
+        const atleta = atletas.find((item) => item.id === inscricao.idAtleta);
         const corrida = corridas.find((item) => item.idCorrida === inscricao.idCorrida);
 
         if (!atleta || !corrida) {
diff --git a/src/app/components/listar-atletas-component/listar-atletas-component.html b/src/app/components/listar-atletas-component/listar-atletas-component.html
index f10d9bd..4d25599 100644
--- a/src/app/components/listar-atletas-component/listar-atletas-component.html
+++ b/src/app/components/listar-atletas-component/listar-atletas-component.html
@@ -19,7 +19,7 @@
         </thead>
 
         <tbody>
-          @for (atleta of atletas(); track atleta.idAtleta) {
+          @for (atleta of atletas(); track atleta.id) {
             <tr>
               <td>{{ atleta.nome }}</td>
 
@@ -42,7 +42,7 @@
               </td>
 
               <td>
-                {{ calcularIdade(atleta.dataNascimento) }} anos
+                {{ calcularIdade(atleta.data_nascimento) }} anos
               </td>
 
               <td>{{ atleta.cidade }}</td>
@@ -53,7 +53,7 @@
                 <button
                   type="button"
                   class="btn-remover"
-                  (click)="removerAtleta(atleta.idAtleta)"
+                  (click)="removerAtleta(atleta.id)"
                 >
                   Remover
                 </button>
diff --git a/src/app/components/listar-atletas-component/listar-atletas-component.ts b/src/app/components/listar-atletas-component/listar-atletas-component.ts
index ffc2bf7..4741433 100644
--- a/src/app/components/listar-atletas-component/listar-atletas-component.ts
+++ b/src/app/components/listar-atletas-component/listar-atletas-component.ts
@@ -48,12 +48,12 @@ export class ListarAtletasComponent implements OnInit {
     return idade;
   }
 
-  removerAtleta(idAtleta?: number) {
-    if (idAtleta === undefined) {
+  removerAtleta(id?: number) {
+    if (id === undefined) {
       return;
     }
 
-    this.atletaService.remover(idAtleta).subscribe(() => {
+    this.atletaService.remover(id).subscribe(() => {
       this.atualizarLista();
     });
   }
diff --git a/src/app/models/pessoa-model.ts b/src/app/models/pessoa-model.ts
index 95b5b7c..fed1e77 100644
--- a/src/app/models/pessoa-model.ts
+++ b/src/app/models/pessoa-model.ts
@@ -1,13 +1,12 @@
 export class Pessoa {
-    // O MockAPI gera este identificador automaticamente ao cadastrar o atleta.
-    idAtleta?: number;
-    nome: string = '';
-    cpf: string = '';
-    sexo: string = '';
-    dataNascimento: string = '';
-    cep: number = 0;
-    rua: string = '';
-    bairro: string = '';
-    cidade: string = '';
-    uf: string = '';
+  id?: number;
+  nome: string = '';
+  cpf: string = '';
+  sexo: string = '';
+  data_nascimento: string = '';
+  cep: number = 0;
+  rua_logradouro: string = '';
+  bairro: string = '';
+  cidade: string = '';
+  uf: string = '';
 }
diff --git a/src/app/services/atleta-service.ts b/src/app/services/atleta-service.ts
index 53c0d2b..8d7b743 100644
--- a/src/app/services/atleta-service.ts
+++ b/src/app/services/atleta-service.ts
@@ -1,32 +1,29 @@
 import { Injectable } from '@angular/core';
 import { HttpClient } from '@angular/common/http';
 import { Observable } from 'rxjs';
-import { map } from 'rxjs/operators';
 import { Pessoa } from '../models/pessoa-model';
 
 @Injectable({
     providedIn: 'root'
 })
 export class AtletaService {
-    private apiUrl = 'https://6a8629109c451dc67a646a59.mockapi.io/Atletas';
+    private readonly apiUrl = 'http://127.0.0.1:8000/pessoa';
 
     constructor(private http: HttpClient) {}
 
     adicionar(pessoa: Pessoa): Observable<Pessoa> {
-        return this.http.post<Pessoa>(this.apiUrl, pessoa);
+        return this.http.post<Pessoa>(`${this.apiUrl}/`, pessoa);
     }
 
     listar(): Observable<Pessoa[]> {
-      return this.http.get<any[]>(this.apiUrl).pipe(
-          map(lista => lista.map(item => ({ ...item, idAtleta: Number(item.idAtleta) })))
-      );
-  }
+        return this.http.get<Pessoa[]>(`${this.apiUrl}/`);
+    }
 
-    remover(idAtleta: number): Observable<void> {
-        return this.http.delete<void>(`${this.apiUrl}/${idAtleta}`);
+    remover(id: number): Observable<void> {
+        return this.http.delete<void>(`${this.apiUrl}/${id}`);
     }
 
     alterar(pessoa: Pessoa): Observable<Pessoa> {
-        return this.http.put<Pessoa>(`${this.apiUrl}/${pessoa.idAtleta}`, pessoa);
+        return this.http.put<Pessoa>(`${this.apiUrl}/${pessoa.id}`, pessoa);
     }
 }

