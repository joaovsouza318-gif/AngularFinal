diff --git a/src/app/app.routes.ts b/src/app/app.routes.ts
index 7a271ec..99e8376 100644
--- a/src/app/app.routes.ts
+++ b/src/app/app.routes.ts
@@ -10,6 +10,7 @@ export const routes: Routes = [
   {path:"home", component:HomeComponent},
   {path:"cadastroAtleta", component:AtletaComponent},
   {path:"cadastroCorrida", component:CorridasComponent},
+  {path:"cadastroCorrida/:id", component:CorridasComponent},
   {path:"listarCorridas", component:ListarCorridasComponent},
   {path:"inscrevaSe", component:InscricaoComponent}
 ];
diff --git a/src/app/components/corridas-component/corridas-component.css b/src/app/components/corridas-component/corridas-component.css
index 7a0ae93..b9f977c 100644
--- a/src/app/components/corridas-component/corridas-component.css
+++ b/src/app/components/corridas-component/corridas-component.css
@@ -21,6 +21,12 @@ body {
   flex-direction: column;
 }
 
+#titulo-form {
+  margin-bottom: 15px;
+  color: var(--preto);
+  text-align: center;
+}
+
 #form-corrida label {
   margin-top: 12px;
   margin-bottom: 5px;
@@ -96,13 +102,15 @@ body {
   background-color: var(--azul);
 }
 
-/* Limpar */
-#btnLimpar {
+/* Limpar / Cancelar */
+#btnLimpar,
+#btnCancelar {
   background-color: var(--vermelho);
   color: var(--branco);
 }
 
-#btnLimpar:hover {
+#btnLimpar:hover,
+#btnCancelar:hover {
   background-color: var(--laranja);
 }
 
diff --git a/src/app/components/corridas-component/corridas-component.html b/src/app/components/corridas-component/corridas-component.html
index 9ff72ab..a453992 100644
--- a/src/app/components/corridas-component/corridas-component.html
+++ b/src/app/components/corridas-component/corridas-component.html
@@ -1,6 +1,8 @@
 <div id="div-form">
   <form id="form-corrida" (ngSubmit)="salvarCorrida()">
 
+    <h2 id="titulo-form">{{ modoEdicao ? 'Editar Corrida' : 'Cadastrar Corrida' }}</h2>
+
     <label for="descricao">Descrição: </label>
     <input type="text" id="descricao" name="descricao" class="input01" required placeholder="Ex: Maratona da Meia-noite..." [(ngModel)]="descricao">
 
@@ -14,8 +16,12 @@
     <input type="radio" name="niveis" id="21" value="21" [(ngModel)]="niveis"> 21 Km
 
     <div id="div-btns">
-      <button type="submit" id="btnAdd">Cadastrar Corrida</button>
-      <button type="button" id="btnLimpar" (click)="limparFormulario()">Limpar Dados</button>
+      <button type="submit" id="btnAdd">{{ modoEdicao ? 'Atualizar Corrida' : 'Cadastrar Corrida' }}</button>
+      @if (modoEdicao) {
+        <button type="button" id="btnCancelar" (click)="cancelarEdicao()">Cancelar</button>
+      } @else {
+        <button type="button" id="btnLimpar" (click)="limparFormulario()">Limpar Dados</button>
+      }
     </div>
   </form>
 </div>
diff --git a/src/app/components/corridas-component/corridas-component.ts b/src/app/components/corridas-component/corridas-component.ts
index 4f11302..dc1b9fc 100644
--- a/src/app/components/corridas-component/corridas-component.ts
+++ b/src/app/components/corridas-component/corridas-component.ts
@@ -1,5 +1,6 @@
-import { Component } from '@angular/core';
+import { ChangeDetectorRef, Component, OnInit } from '@angular/core';
 import { FormsModule } from '@angular/forms';
+import { ActivatedRoute, Router } from '@angular/router';
 import { CorridaService } from '../../services/corrida-service';
 import { Corrida } from '../../models/corrida-model';
 
@@ -9,27 +10,81 @@ import { Corrida } from '../../models/corrida-model';
   templateUrl: './corridas-component.html',
   styleUrl: './corridas-component.css',
 })
-export class CorridasComponent {
+export class CorridasComponent implements OnInit {
+  idCorrida: number | null = null;
   descricao = '';
   data = '';
   niveis = '';
+  modoEdicao = false;
 
-  constructor(private corridaService: CorridaService) {}
+  constructor(
+    private corridaService: CorridaService,
+    private route: ActivatedRoute,
+    private router: Router,
+    private cdr: ChangeDetectorRef,
+  ) {}
 
-  salvarCorrida(){
-    const novaCorrida = new Corrida();
-    novaCorrida.descricao = this.descricao;
-    novaCorrida.data = this.data ? new Date(`${this.data}T00:00:00`) : null;
-    novaCorrida.niveis = this.niveis;
+  ngOnInit() {
+    const idParam = this.route.snapshot.paramMap.get('id');
 
-    this.corridaService.adicionar(novaCorrida).subscribe(() => {
-      this.limparFormulario();
+    if (idParam) {
+      const id = Number(idParam);
+      this.modoEdicao = true;
+
+      this.corridaService.buscarPorId(id).subscribe(corrida => {
+        this.idCorrida = corrida.idCorrida;
+        this.descricao = corrida.descricao;
+        this.data = this.formatarDataParaInput(corrida.data);
+        this.niveis = corrida.niveis;
+
+        // Sem zone.js (app zoneless), o Angular não percebe sozinho essa
+        // mudança vinda de um subscribe assíncrono; forçamos a atualização
+        // da tela para os campos do ngModel refletirem os dados carregados.
+        this.cdr.detectChanges();
+      });
+    }
+  }
+
+  salvarCorrida() {
+    const corrida = new Corrida();
+    corrida.idCorrida = this.idCorrida ?? 0;
+    corrida.descricao = this.descricao;
+    corrida.data = this.data ? new Date(`${this.data}T00:00:00`) : null;
+    corrida.niveis = this.niveis;
+
+    const operacao = this.modoEdicao
+      ? this.corridaService.alterar(corrida)
+      : this.corridaService.adicionar(corrida);
+
+    operacao.subscribe(() => {
+      if (this.modoEdicao) {
+        this.router.navigate(['/listarCorridas']);
+      } else {
+        this.limparFormulario();
+      }
     });
-}
+  }
+
+  cancelarEdicao() {
+    this.router.navigate(['/listarCorridas']);
+  }
 
   limparFormulario() {
     this.descricao = '';
     this.data = '';
     this.niveis = '';
   }
+
+  private formatarDataParaInput(data: Date | string | null): string {
+    if (!data) {
+      return '';
+    }
+
+    const d = new Date(data);
+    const ano = d.getFullYear();
+    const mes = String(d.getMonth() + 1).padStart(2, '0');
+    const dia = String(d.getDate()).padStart(2, '0');
+
+    return `${ano}-${mes}-${dia}`;
+  }
 }
diff --git a/src/app/components/inscricao-component/inscricao-component.html b/src/app/components/inscricao-component/inscricao-component.html
index a3918b7..fc0a337 100644
--- a/src/app/components/inscricao-component/inscricao-component.html
+++ b/src/app/components/inscricao-component/inscricao-component.html
@@ -1,14 +1,14 @@
 <section class="inscricoes">
   <h1>Inscrição do atleta</h1>
 
-  @if (atletas.length === 0 || corridas.length === 0) {
+  @if (atletas().length === 0 || corridas().length === 0) {
     <p class="aviso">Cadastre pelo menos um atleta e uma corrida antes de realizar a inscrição.</p>
   } @else {
     <form (ngSubmit)="salvarInscricao()">
       <label for="atleta">Atleta</label>
       <select id="atleta" name="atleta" [(ngModel)]="idAtleta" required>
         <option [ngValue]="null" disabled>Selecione um atleta</option>
-        @for (atleta of atletas; track atleta.idPessoa) {
+        @for (atleta of atletas(); track atleta.idPessoa) {
           <option [ngValue]="atleta.idPessoa">{{ atleta.nome }}</option>
         }
       </select>
@@ -16,7 +16,7 @@
       <label for="corrida">Corrida</label>
       <select id="corrida" name="corrida" [(ngModel)]="idCorrida" required>
         <option [ngValue]="null" disabled>Selecione uma corrida</option>
-        @for (corrida of corridas; track corrida.idCorrida) {
+        @for (corrida of corridas(); track corrida.idCorrida) {
           <option [ngValue]="corrida.idCorrida">{{ corrida.descricao }} — {{ corrida.niveis }} Km</option>
         }
       </select>
@@ -26,7 +26,7 @@
   }
 
   <h2>Inscrições realizadas</h2>
-  @if (inscricoes.length === 0) {
+  @if (inscricoes().length === 0) {
     <p class="aviso">Nenhuma inscrição realizada até o momento.</p>
   } @else {
     <div class="tabela-responsiva">
@@ -35,7 +35,7 @@
           <tr><th>Atleta</th><th>Corrida</th><th>Data</th><th>Modalidade</th></tr>
         </thead>
         <tbody>
-          @for (inscricao of inscricoes; track inscricao.id) {
+          @for (inscricao of inscricoes(); track inscricao.id) {
             <tr>
               <td>{{ inscricao.atleta }}</td>
               <td>{{ inscricao.corrida }}</td>
diff --git a/src/app/components/inscricao-component/inscricao-component.ts b/src/app/components/inscricao-component/inscricao-component.ts
index d758204..355809f 100644
--- a/src/app/components/inscricao-component/inscricao-component.ts
+++ b/src/app/components/inscricao-component/inscricao-component.ts
@@ -1,5 +1,5 @@
 import { CommonModule } from '@angular/common';
-import { Component, OnInit } from '@angular/core';
+import { Component, OnInit, signal } from '@angular/core';
 import { FormsModule } from '@angular/forms';
 import { forkJoin } from 'rxjs';
 import { Corrida } from '../../models/corrida-model';
@@ -24,9 +24,9 @@ interface InscricaoExibicao {
   styleUrl: './inscricao-component.css',
 })
 export class InscricaoComponent implements OnInit {
-  atletas: Pessoa[] = [];
-  corridas: Corrida[] = [];
-  inscricoes: InscricaoExibicao[] = [];
+  atletas = signal<Pessoa[]>([]);
+  corridas = signal<Corrida[]>([]);
+  inscricoes = signal<InscricaoExibicao[]>([]);
   idAtleta: number | null = null;
   idCorrida: number | null = null;
 
@@ -41,8 +41,8 @@ export class InscricaoComponent implements OnInit {
       atletas: this.atletaService.listar(),
       corridas: this.corridaService.listar(),
     }).subscribe(({ atletas, corridas }) => {
-      this.atletas = atletas;
-      this.corridas = corridas;
+      this.atletas.set(atletas);
+      this.corridas.set(corridas);
       this.atualizarInscricoes();
     });
   }
@@ -62,9 +62,12 @@ export class InscricaoComponent implements OnInit {
   }
 
   private atualizarInscricoes() {
-    this.inscricoes = this.inscricaoService.listar().flatMap((inscricao) => {
-      const atleta = this.atletas.find((item) => item.idPessoa === inscricao.idAtleta);
-      const corrida = this.corridas.find((item) => item.idCorrida === inscricao.idCorrida);
+    const atletas = this.atletas();
+    const corridas = this.corridas();
+
+    const lista = this.inscricaoService.listar().flatMap((inscricao) => {
+      const atleta = atletas.find((item) => item.idPessoa === inscricao.idAtleta);
+      const corrida = corridas.find((item) => item.idCorrida === inscricao.idCorrida);
 
       if (!atleta || !corrida) {
         return [];
@@ -78,5 +81,7 @@ export class InscricaoComponent implements OnInit {
         modalidade: corrida.niveis,
       }];
     });
+
+    this.inscricoes.set(lista);
   }
 }
diff --git a/src/app/components/listar-corridas-component/listar-corridas-component.css b/src/app/components/listar-corridas-component/listar-corridas-component.css
index 523d43c..85734e7 100644
--- a/src/app/components/listar-corridas-component/listar-corridas-component.css
+++ b/src/app/components/listar-corridas-component/listar-corridas-component.css
@@ -39,6 +39,12 @@ th {
   color: var(--branco);
 }
 
+.acoes {
+  display: flex;
+  gap: 8px;
+  flex-wrap: wrap;
+}
+
 button {
   padding: 8px 12px;
   color: var(--branco);
@@ -52,3 +58,11 @@ button:hover {
   background: var(--laranja);
   color: var(--preto);
 }
+
+.btn-editar {
+  background: var(--azul);
+}
+
+.btn-editar:hover {
+  background: var(--roxo);
+}
diff --git a/src/app/components/listar-corridas-component/listar-corridas-component.html b/src/app/components/listar-corridas-component/listar-corridas-component.html
index fe91411..1d1c920 100644
--- a/src/app/components/listar-corridas-component/listar-corridas-component.html
+++ b/src/app/components/listar-corridas-component/listar-corridas-component.html
@@ -20,7 +20,12 @@
               <td>{{ corrida.descricao }}</td>
               <td>{{ corrida.data | date: 'dd/MM/yyyy' }}</td>
               <td>{{ corrida.niveis }} Km</td>
-              <td><button type="button" (click)="removerCorrida(corrida.idCorrida)">Remover</button></td>
+              <td>
+                <div class="acoes">
+                  <button type="button" class="btn-editar" (click)="editarCorrida(corrida.idCorrida)">Editar</button>
+                  <button type="button" class="btn-remover" (click)="removerCorrida(corrida.idCorrida)">Remover</button>
+                </div>
+              </td>
             </tr>
           }
         </tbody>
diff --git a/src/app/components/listar-corridas-component/listar-corridas-component.ts b/src/app/components/listar-corridas-component/listar-corridas-component.ts
index 9cf1e80..a0e37f7 100644
--- a/src/app/components/listar-corridas-component/listar-corridas-component.ts
+++ b/src/app/components/listar-corridas-component/listar-corridas-component.ts
@@ -1,5 +1,6 @@
 import { CommonModule } from '@angular/common';
 import { Component, OnInit, signal } from '@angular/core';
+import { Router } from '@angular/router';
 import { Corrida } from '../../models/corrida-model';
 import { CorridaService } from '../../services/corrida-service';
 
@@ -12,12 +13,19 @@ import { CorridaService } from '../../services/corrida-service';
 export class ListarCorridasComponent implements OnInit {
   corridas = signal<Corrida[]>([]);
 
-  constructor(private corridaService: CorridaService) {}
+  constructor(
+    private corridaService: CorridaService,
+    private router: Router,
+  ) {}
 
   ngOnInit() {
     this.atualizarLista();
   }
 
+  editarCorrida(idCorrida: number) {
+    this.router.navigate(['/cadastroCorrida', idCorrida]);
+  }
+
   removerCorrida(idCorrida: number) {
     this.corridaService.remover(idCorrida).subscribe(() => {
       this.atualizarLista();
diff --git a/src/app/services/corrida-service.ts b/src/app/services/corrida-service.ts
index e836534..e327174 100644
--- a/src/app/services/corrida-service.ts
+++ b/src/app/services/corrida-service.ts
@@ -22,6 +22,12 @@ export class CorridaService {
       );
   }
 
+    buscarPorId(idCorrida: number): Observable<Corrida> {
+        return this.http.get<any>(`${this.apiUrl}/${idCorrida}`).pipe(
+            map(item => ({ ...item, idCorrida: Number(item.idCorrida) }))
+        );
+    }
+
     remover(idCorrida: number): Observable<void> {
         return this.http.delete<void>(`${this.apiUrl}/${idCorrida}`);
     }
